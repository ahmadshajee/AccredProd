import EmployerDashboard from './EmployerDashboard';

export default function VerifierDashboard(props) {
  // The verifier functionality is identical to the employer functionality for now
  return <EmployerDashboard {...props} />;
}
